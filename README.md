# Range
A simple range implementation with utility methods

## Some of the methods
`clamp()`
`randomNumber()`
`randomInteger()`
`isNumberWithin()`
`isNumberExclusivelyWithin()`
`isRangeWithin()`
`isRangeExclusivelyWithin()`
...and more